# cfg.example.env - Vorlage fuer sql-contained-users/. Kopieren zu cfg.<env>.env und ausfuellen;
# das Skript sourcet cfg.<env>.env anhand des ersten Arguments (test|acc|prod).
# cfg.<env>.env NICHT committen (siehe .gitignore). Restliche Namen werden aus
# <env> abgeleitet (WORKLOAD/INDEX per ENV ueberschreibbar).
CFG_TENANT_ID="00000000-0000-0000-0000-000000000000"
